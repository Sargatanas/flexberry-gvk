import timeString from 'ember-app/utils/time-string';
import { module, test } from 'qunit';

module('Unit | Utility | time string');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = timeString();
  assert.ok(result);
});
