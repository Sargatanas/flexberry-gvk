import getTasks from 'ember-app/utils/get-tasks';
import { module, test } from 'qunit';

module('Unit | Utility | get tasks');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getTasks();
  assert.ok(result);
});
