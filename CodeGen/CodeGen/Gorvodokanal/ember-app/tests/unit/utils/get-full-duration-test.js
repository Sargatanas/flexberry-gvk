import getFullDuration from 'ember-app/utils/get-full-duration';
import { module, test } from 'qunit';

module('Unit | Utility | get full duration');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getFullDuration();
  assert.ok(result);
});
