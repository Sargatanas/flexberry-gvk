import { getTimeString } from 'ember-app/helpers/get-time-string';
import { module, test } from 'qunit';

module('Unit | Helper | get time string');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getTimeString([42]);
  assert.ok(result);
});
