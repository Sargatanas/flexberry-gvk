import { getDateTimeString } from 'ember-app/helpers/get-date-time-string';
import { module, test } from 'qunit';

module('Unit | Helper | get date time string');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getDateTimeString([42]);
  assert.ok(result);
});
