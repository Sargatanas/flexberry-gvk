import { getTaskList } from 'ember-app/helpers/get-task-list';
import { module, test } from 'qunit';

module('Unit | Helper | get task list');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getTaskList([42]);
  assert.ok(result);
});
