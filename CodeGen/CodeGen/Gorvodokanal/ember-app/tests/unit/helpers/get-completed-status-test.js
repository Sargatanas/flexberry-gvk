import { getCompletedStatus } from 'ember-app/helpers/get-completed-status';
import { module, test } from 'qunit';

module('Unit | Helper | get completed status');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getCompletedStatus([42]);
  assert.ok(result);
});
